---
name: "🔖 Pull Request Labeler"

on:
  pull_request_target:
  workflow_dispatch: {}

permissions:
  contents: read
  pull-requests: write

jobs:
  labels:
    name: "🚀 Run Labeler"
    uses: z-shell/.github/.github/workflows/labeler.yml@main
